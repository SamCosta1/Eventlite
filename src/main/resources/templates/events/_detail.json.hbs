{
	"title": "EventLite event view",
	"_self": "{{self_link}}events/{{event.id}}",
	"id": "{{event.id}}",
	"date" : "{{event.date}}",
	"name": "{{event.name}}",
	 "venue": { {{#venue}}
	  "id": "{{id}}",
	  "name": "{{name}}",
	  "capacity": "{{capacity}}",
	  "_self":"{{self_link}}venues/{{id}}"
	{{/venue}} }
}
